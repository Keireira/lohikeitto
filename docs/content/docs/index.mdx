---
title: Overview
description: Lohikeitto — Service catalog API
---

<Callout type="warn">
This documentation was auto-generated by Claude (Anthropic AI) and may contain inaccuracies. Always verify against the actual source code.
</Callout>

Lohikeitto (Soup) is a service catalog API. It powers subscription search in Sharkie — users type a service name and get back logos, domains, and metadata from multiple sources.

Full endpoint documentation is available in the [API Reference](/api-reference).

## How search works

When a client sends `GET /search?q=spotify`, the API searches up to five sources **in parallel**:

```
           ┌─── Inhouse DB (PostgreSQL, pg_trgm) ── ~50ms
           │
 request ──┼─── App Store (iTunes Search API) ────── ~300ms
           │
           ├─── Google Play (HTML parsing) ────────── ~400ms
           │
           ├─── Brandfetch API ───────────────────── ~500ms
           │
           └─── logo.dev API ─────────────────────── ~800ms
```

The `sources` parameter controls which sources are queried — as a comma-separated list:

```
/search?q=spotify&sources=inhouse,appstore,playstore
```

Aliases: `external` (brandfetch + logodev + appstore + playstore), `mobile` (appstore + playstore), `all` (default — all five).

Results are **deduplicated by domain** with priority:

```
inhouse  >  appstore  >  playstore  >  brandfetch  >  logo.dev
```

If `spotify.com` appears in all five — only the inhouse version is kept. Mobile store results use package/bundle IDs as their identifier (e.g. `com.spotify.client`), so they don't conflict with domain-based results from other sources.

Inhouse (curated) services may have multiple domains — all of them are searchable and each one blocks external duplicates. External results always have a single domain and are **never merged** with each other to prevent search poisoning.

When 3+ sources return data, each external source is capped at **5 results** to avoid flooding. Inhouse results are not capped.

### Mobile store metadata

App Store and Google Play results carry extra metadata: `description`, `bundle_id`, `seller_name`, `seller_domain` (App Store only, from `sellerUrl`), `category_slug` (genres mapped to local categories), and `tags`. These fields are `null` for other sources.

Google Play has no public JSON API — results are extracted by parsing the Play Store HTML pages (meta tags + embedded data).

### Service lookup

`GET /service/{lookup}` resolves a service by UUID, slug, domain, or bundle ID. If not found locally, it searches external sources and stashes the result in the **limbus** (approval queue).

For mobile apps, use `?source_hint=` to trigger an exact lookup by package/bundle ID:

```
/service/org.getpure.pure-iphone?source_hint=appstore
/service/com.whatsapp?source_hint=playstore
```

Without the hint, the lookup falls back to fuzzy search across all external sources.

### Frontend strategy

The client fires two sequential requests to keep the UI responsive:

1. `GET /search?q=...&sources=inhouse` — renders instantly (~50ms)
2. `GET /search?q=...&sources=external` — appends results when ready (~1-2s)

External results use a **deterministic UUID v5** derived from the domain (or bundleId for App Store), so React keys stay stable across requests.

## Quick Start

```bash
cp lohikeitto.env.example lohikeitto.local.env
# fill in API keys (Brandfetch, logo.dev, Cloudflare R2)

make dev
```

Migrations run automatically on startup. The database is created if it doesn't exist.