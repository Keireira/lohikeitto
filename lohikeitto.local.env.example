# Local development overrides (copy to sharkie.local.env)
# Values here take precedence over sharkie.env
# sharkie.local.env is gitignored — safe for local secrets

DATABASE_URL=postgresql://sharkie:sharkie_123@localhost:5432/lohikeitto
ALLOWED_ORIGINS=http://localhost:3001,http://localhost:3333
RUST_LOG=lohikeitto=debug,sqlx=warn,tower_http=debug

ADMIN_TOKEN=
